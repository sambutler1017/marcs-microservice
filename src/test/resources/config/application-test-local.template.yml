spring:
  datasource:
    username:
    password:
    url: jdbc:mysql://localhost:3306
    main:
      web-application-type: none

security:
  allowed:
    domains: "*"
  signing-key: "test-local-key"
